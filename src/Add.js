/*import React, { Component } from "react";
import { ContactAdd } from './App';

class Add extends Component {

    render() {
        
        let formAdd = this.props.entries.map(entry => (<ContactAdd key={entry.key} entry={entry} add={this.props.add} /> 

        ));

        return (
            <ul className="ui relaxed divided list selection">
                {formAdd}
            </ul>
        )
    }
};

export default Add;*/